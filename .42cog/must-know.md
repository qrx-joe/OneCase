# must-know.md —— 待拍板决策

> 系统分析产物。每个决策点 = 不可妥协 + 我要作的决定 + 排除项。
> 拍板后移入 `docs/adr/`，此处只留未决。分析服务于决定：能列出「需要拍板的地方」就够了，列出来就停，不写综述。

## 决策一：真实视觉模型验收 —— 图片识别怎么摘掉「未验收」

**进展**（2026-09-05）：预算授权已由本人解除（无需逐轮确认）；首发验收对象 = StepFun（当前运行态）；探索轮按 `docs/testing/real-model-quality.md` 规程执行（synthetic-facts-v1：20 文字 + 10 合成图片，单轮 30 次调用上限）。结果与剩余待定项见 devlog 最新记录。**仍待拍板**：自动字段检查之外的验收通过线与人工审核结论——探索轮不预设门槛，需本人看过失败样本后再定。

**不可妥协**：
1. 识别结果必须过 Zod runtime 校验才进入人工确认流程——输入是 untrusted data，这条不动摇。
2. 人工确认闸门不可绕过：AI 结果永远是 Draft。
3. 无有效 key / 调用失败时明确返回「不能识别」；任何情况下不用 Mock 结果冒充识别成功。

**排除项**（知道了也不改变决定）：模型单价对比细节、语音转写、并发性能、微信入口。

## ~~决策二：SQLite → PostgreSQL 迁移时机~~

已拍板（2026-09-05）：参赛阶段不动，试点启动时迁；触发条件 = 试点承诺或多用户并发写入的真实需求。→ [ADR-003](../docs/adr/003-sqlite-postgresql-timing.md)
