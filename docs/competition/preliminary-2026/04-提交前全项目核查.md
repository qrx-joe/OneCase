# OneCase 初赛提交前核查

> 历史快照：本文保留当时的附件、功能和测试记录，不能作为当前提交依据。团队名称已停用，姓名和电话不放入 PPT/PDF；图片输入已修复。请使用 [最新提交说明](README.md) 和个人参赛版文案，现有二进制附件仍需同步导出。

检查日期：2026-08-31。
结论：已有完整 v1 PPTX／PDF，格式和模块基本符合初赛要求；尚需补齐个人联系信息、核实本人声明、准备实际运行演示材料。无需为本次初赛临时扩建图片、语音、微信接入或完整权限系统。

本报告是检查快照，不是已提交记录或全量安全验收。未修改产品代码、数据库、配置、已有 PPT/PDF，未向外发送材料。

## 1. 当前可以使用的附件

所在目录：`D:\code\ONE-CASE\docs\competition\preliminary-2026`。

| 文件 | 检查结果 |
|---|---|
| `AI+民生-不理不器-OneCase-v1.pptx` | 已存在，303273 字节，14 页，16:9 |
| `AI+民生-不理不器-OneCase-v1.pdf` | 已存在，389393 字节，14 页，横版约 16:9 |

检查方法与边界：

- 读取 PPTX 内部页面文字、页面尺寸与演讲者备注；检查 PDF 的全部页面文字、尺寸，并逐页查看 PDF 渲染。
- 封面 1 页、主体 12 页、结束／问答 1 页，主体未超过 15 页。
- 五个必含模块齐全：定位第 1 页；团队第 3 页；方案／Demo 第 4—8 页；案例与背书第 9 页；商业与落地第 10—11 页。
- 未发现可见正文中的“待补／待确认”占位符；正式事项“待确认”作为业务状态不属于资料占位符。
- 去除空白后，PPTX 与 PDF 每页文字字符及数量一致；这不是文本顺序或对象布局完全相同的证明。
- PDF 逐页未见明显乱码、文字截断或内容遮挡。PPTX 本次未在桌面 PowerPoint 中播放，外接投影效果仍需现场前检查。
- 第 3、9 页部分灰色小字对投影设备较敏感，排练时需确认后排可读；不必为此推倒重做全套设计。

检查版本 SHA-256：

```text
pptx 55ee62f19587925a8858a624520bb23f3c5e6963d20ea71da16a8ff2c5115fa0
pdf  d8ec8b868c3fb6b2eb6ff07cddc078d8f9700d30b4630a3644c3f20ae1a27bc0
```

`projects/onecase-prelim-v2_ppt169_20260831` 是另一套制作工程。检查时该工程仅发现封面 SVG，未发现完整导出的 PPTX/PDF；不能把它当成已完成附件，也不要将设计规格或 SVG 工程目录发给评委代替正式文件。若后续完成 v2，需要重新导出并核对配套 PPTX/PDF。

## 2. 提交前必须处理的事项

| 优先级 | 待办 | 现状与处理方式 |
|---|---|---|
| 必须 | 补联系方式 | 现有 PPT/PDF 没有联系电话；清单仍为占位符。至少在邮件签名补上，团队介绍或末页也建议保留可联系信息 |
| 必须 | 个人信息与报名记录一致 | 当前文件填写“AI+民生／不理不器／乔瑞雪／1 人”；学校或公司、专业或职务、年级仍未补。按实际身份填写，不适用项如实处理，不虚构单位 |
| 必须 | 核实第 13 页的声明 | 页面已写“全部功能实现与测试均由本人独立完成；项目未公开发表、未商业化”。本次文件检查不能独立证明这些事实；需要本人确认与真实贡献、发布和商业状态一致。不符合时先纠正文案，涉及资格口径请主办方确认 |
| 必须 | 确认已经报名 | 未发现报名成功凭证；这不等于没有报名。核对自己的报名记录，赛前邮件不能代替报名 |
| 必须 | 最终选择同一套附件 | 现有 v1 可以作为定稿基础；不要混发 v1 PPT 和 v2 PDF，修改 PPT 后必须重新导出 PDF |

本人贡献建议表述（仅在符合事实时使用）：

> 本人负责产品定义、业务规则设计、工程实现组织与最终验收；开发中使用 AI 编程工具辅助编码和测试。框架、模型和第三方组件按来源说明。

“未公开发表、未商业化”应独立核实，不因已经写进 PPT 就视为已确认。

## 3. 演示材料：最值得补的缺口

### 3.1 三张实际运行截图

现有第 6 页是操作步骤，没有产品运行截图。现有 PNG/JPG 主要是幻灯片预览，不是 OneCase 的运行证据。

建议从独立、合成数据的 Demo 环境取得三张截图，用于改进方案／Demo 页或制作备用材料：

1. Review 页：同一条文字产生照明、清运两个草稿，同时显示相似候选。
2. 确认结果或 Case Detail：照明关联已有事项，清运新建，居民来源及处理记录发生变化。
3. 首页：确认后的正式事项统计，保留 Demo 数据标识。

截图应注明运行版本、日期、实际 Provider 与合成数据属性。不要把 PPT 概念图、UI 设计稿、模拟返回值截图写成真实模型效果证明。

### 3.2 90—120 秒离线备用视频

检查范围内未发现可直接使用的产品演示 MP4/WebM。录屏不是细则要求的独立邮件附件，但对现场断网、切屏或模型失败很有帮助。

录制内容：原始文字 → 两个草稿 → 关联一个、新建一个 → 来源／处理记录 → 统计变化。视频与现场演示均须如实标明 Mock 或真实 Provider。

### 3.3 确定现场 AI 模式

- 代码已具备 Mock／Qwen／OpenAI 适配，不能据此断言真实服务已验证。
- 若主要演示 Mock：口径应为“流程演示／工程验证”，不要称其为真实模型推理或准确率验证。
- 若现场演示真实模型：赛前准备合成样例实跑记录，包括 Provider、模型、日期、耗时、原始输出、失败和人工修改；保留已录视频或明确标识的 Mock 备用路径。
- 当前缺少真实模型效果评估材料，不应在邮件中写“高准确率”“已经验证大幅提效”。

### 3.4 排练与设备

完成一次 12 分钟以内的完整排练，额外练习 5 分钟问答；检查笔记本、电源、外接显示和离线打开附件。原有讲稿按 10 分 30 秒编排，可保留约 90 秒切屏缓冲。

## 4. 项目实现与宣传口径

| 范围 | 目前可核对的实现 | 材料如何表述 |
|---|---|---|
| 文字 Intake | 前后端均限制 10000 个 UTF-16 代码单元；创建接口校验输入与幂等冲突 | 可讲“文字输入与长度校验已实现”；不能再笼统说没有输入限制 |
| 草稿及确认 | 可编辑标题、地点、优先级；新建／关联／拒绝；确认要求 ANALYZED、COMPLETED | 可讲“人工确认后进入正式事项，确认决策有事务和留痕” |
| AI | 多 Provider 适配、结构校验、有限重试、失败路径 | 讲接口与工程机制；真实模型效果待专项证据 |
| 重复候选 | 标题词汇、地点、类别、时间排序，默认 Top3 | 不能称已接入 Embedding，不能把分数称为重复概率 |
| 事项跟踪 | 来源、时间线、状态迁移、统计 | 演示数据统计不是客户成绩或业务提效 |
| 恢复入口 | 已有带 Intake ID 的刷新恢复及旧 URL 跳转 | 不应说所有草稿完全无法恢复；仍缺工作台上的待确认草稿队列、丢弃终态 |
| 多模态及渠道 | 图片／语音为未接通的入口样式；未接微信 | 限于文字演示，不操作未实现入口 |
| 安全与部署 | 详情与状态变更尚未按认证身份校验归属，缺完整认证、权限、频控 | 只讲受控演示；不应为了发邮件临时公开部署无保护的服务 |

代码证据：`apps/web/src/app/api/intakes/route.ts`、`packages/contracts/src/schemas.ts`、`apps/web/src/lib/confirm-intake-service.ts`、`apps/web/src/lib/ai-provider.ts`、`apps/web/src/lib/duplicate-service.ts`、`apps/web/src/app/intake/page.tsx`、`apps/web/src/app/intake/[id]/page.tsx`、Case／Intake 详情及状态 API。

### 与现场相关的另外两处代码风险

以下为本次只读代码检查发现，尚未运行复现，也未修改代码：

1. `packages/ai/src/openai-compatible.ts:109` 在 fetch 返回后即清理超时定时器，而 `res.text()`／`res.json()` 在其后。响应头已返回但正文迟迟不完成时，当前定时器不能继续中止正文读取。真实 Provider 演示需验证该慢响应场景并准备备用路径，不能宣称任何请求都严格受 30 秒总时限控制。
2. `apps/web/src/app/api/cases/[id]/status/route.ts:58` 先读并比较版本，随后 `tx.case.update` 的条件只有 id，没有 version。并发请求若都通过前置校验，存在覆盖状态的风险。不能将现有 version 字段表述为“已经证明防止所有并发覆盖”；单人顺序 Demo 不等于多人协作验收。

这些不是初赛独立附件要求。优先保证展示范围真实、演示稳定；涉及多人或公开试点前再处理对应工程验证。

## 5. 测试证据怎么使用

第 9 页写有 116 项单元测试、87 项业务不变量、22 项 E2E 通过。与 `docs/devlog/CODE_REVIEW_2026-08-31.md` 第 9 节及 `03-Demo与答辩准备.md` 的后续记录一致。

本次还检查到 `apps/web/test-results/.last-run.json` 为 passed，failedTests 为空；该文件本身不包含测试数、命令、Provider 或代码版本，不能单独证明“22 项”的完整范围。

本次没有重新执行测试、构建、浏览器交互或远程 CI。上述数字应称为“2026-08-31 已有本地测试记录”，不要称为本轮全部复验结果。

建议保留一份便于答辩的内部测试摘要：日期、代码版本／未提交改动、命令、用例数量、结果、运行模式与失败记录。不是组委会必交附件。若继续改代码，至少复跑相关用例与黄金链路后再沿用数字。

特别注意：现有 Playwright 配置会复用 3000 端口服务，测试用例会调用 reset。不要直接对正在演示或保存重要数据的数据库运行整套 E2E，应先隔离服务和数据库。

## 6. 开源依赖标注

已重新读取根目录及 apps/packages 的 package.json，对声明的直接第三方依赖按包名去重，读取本机已安装元数据，共 27 个：MIT 21、Apache-2.0 5、ISC 1。包含开发工具与未实际进入产品页面的 UI 包依赖；不是“27 个生产依赖”。

| License | 直接依赖包 |
|---|---|
| MIT | @radix-ui/react-dialog、@radix-ui/react-select、@radix-ui/react-tabs、@radix-ui/react-toast、@types/node、@types/react、@types/react-dom、autoprefixer、clsx、date-fns、next、postcss、prettier、prettier-plugin-tailwindcss、react、react-dom、tailwind-merge、tailwindcss、tsx、vitest、zod |
| Apache-2.0 | @playwright/test、@prisma/client、class-variance-authority、prisma、typescript |
| ISC | lucide-react |

第 13 页主要组件标注与元数据相符。此核查不覆盖全部传递依赖、字体、素材、工具自身依赖，也不是完整许可证合规结论。v2 若使用新图标等素材，应另核对其来源和标注，不能套用本表。

## 7. 文档需要同步的地方

- `01-报名信息与提交清单.md` 顶部写已生成，但后文仍有“尚未生成”；姓名等表格部分仍待补。
- `02-初赛PPT逐页文案与讲稿.md` 仍有旧状态及待补案例，和已生成 PPT 的正文不完全同步。
- `03-Demo与答辩准备.md` 同时保留 103／67 旧记录和后续 116／87／22 新记录，应按日期与记录用途区分。
- v1 第 12 页“输入与访问限制”建议准确写为“访问频控与权限”；“草稿找回”建议写为“待确认草稿队列”。长度校验、带 ID 的刷新恢复已实现。
- v1 第 9 页“暂无客户与合作数据”和第 13 页个人声明，均应由本人确认，不以软件测试推断商业或原创状态。

本次新增本报告和独立邮件草稿，未覆盖其他正在编辑的文案或设计工程。

## 8. 实际发送包

**官方必交附件只有 PPTX 和配套 PDF，各一份。** 不需要额外发送源码压缩包、完整 PRD、内部审查报告、所有测试日志或身份证扫描件。项目摘要、团队信息、概念图和开源标注可在 PPT／邮件中体现；作品集链接为加分项，有且适合公开时再补。

目前优先级：

1. 补个人联系信息，核实报名与第 13 页声明。
2. 选择定稿版本，同步 PPT／PDF。
3. 能补则补真实运行截图，准备本地备用录屏；不要因此错过提交期限。
4. 按《05-提交邮件草稿.md》发送两份附件，并保存发送记录、争取取得收件确认。

截止原文为“9 月 1 日前”，没有具体时刻；应按 8 月 31 日完成发送准备，不擅自延后到 9 月 1 日晚。若已临近或超过时限，应直接向组委会确认，不假定可以补交。
