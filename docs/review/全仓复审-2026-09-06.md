# 全仓复审报告（文档 + 代码）— 2026-09-06

> 评审方式：换谱系对抗性评审（干净上下文会话，按 `cross-lineage-review` 协议）。四路并行探查（文档 / packages / apps-web / 测试与CI）+ 关键发现逐条人工核实 + 全量实测复跑。行号以当日工作区为准。
> 驳回线：凡削弱「AI 结果可编辑 / 人工确认闸门 / 不虚构数字」的意见一律不采纳——本报告无此类意见。

## 一、总体结论

**业务代码无 P0。文档声称的口径（213 单测 / 99 不变量 / 32 E2E）本会话全量实测吻合、全绿。**发现集中在两类：① 不变量脚本的运行条件与文档描述不符（P1）；② 演示材料数字口径分裂与旧材料残留（P1/P2，材料轮本来就要刷）。演示主路径（登录→来件→分析→确认→状态→看板）代码审查未发现阻塞性缺陷。

## 二、实测验证（本会话 2026-09-06 复跑）

| 项 | 声称 | 实测 | 结论 |
|---|---|---|---|
| 单测 | 213（33/25/74/81） | 33+25+74+81 = **213 全过** | ✅ 以 vitest 实跑为准 |
| 业务不变量 | 99/99 | **99/99** | ✅（运行条件见 P1-2） |
| Playwright E2E | 32/32 | **32 passed (1.7m)** | ✅ |
| typecheck | 通过 | 通过 | ✅ |
| next build | 通过 | 本轮未跑（:3000 dev 在运行，build 共写 .next 会踩意外矩阵坑） | ⏸ 由 CI 覆盖 |

**误报记录**：测试审查代理静态清点得出「单测 208 / E2E 31」并报 P0，被实跑推翻（it.each 展开数错）。教训：用例数一律以 vitest/playwright 实跑输出为准，静态 grep 只能做粗筛。

## 三、发现清单

### P1（应修，演示/材料轮前）

1. **不变量脚本运行条件与文档不符**（已人工核实）。README:133、preliminary-2026/README:37、docs/testing/image-intake.md:26、个人参赛版/08 Q15 等多处称不变量跑在「独立临时 SQLite、强制 Mock、演示库未动」。实测：`apps/web/scripts/test-confirm-invariants.mts` 结尾执行 `db:reset`，重置的是演示库 `file:./dev.db`（日志原文「Demo 基线恢复」）；Mock 依赖 tsx 环境缺省（`tsx -e` 实测看不到 `.env.local` 的 `AI_PROVIDER`），**并未强制**——若 shell 导出 stepfun 变量，脚本会真实外呼。修法：脚本装配 Provider 前强制 `AI_PROVIDER='mock'`（并在 CLAUDE.md 意外矩阵注明该脚本也会重置演示库），文档措辞统一改为「自带 reset（重置演示库）+ 依赖缺省 Mock」。
2. **演示材料数字口径分裂**。`01-报名信息与提交清单.md:3` 与 preliminary-2026/README:7 称 200/29 为「2026-09-06 最新实测口径」，与同日 06/07/08/根README 的 213/32 冲突；02-v2 口播稿（:217）仍说「200 项…29 项端到端」且无「PPT P9 快照 200」的答辩口径注记（该注记在 06/08 有）。board 已把材料刷新排给材料轮，但 01 的「最新」措辞会让人误判材料已同步。修法：01、preliminary-2026/README、02-v2 头部刷为 213/32 并统一注「PPT P9 快照 200，代码实测 213」。

### P2（建议修，不阻塞演示）

3. **「禁本地存储死循环」修复只覆盖了一半**（已人工核实）。`demo-auth.ts:31,43` 的 `typeof localStorage === 'undefined'` 只挡 SSR；Chrome「阻止所有 Cookie」下访问 `localStorage` getter 本身抛 SecurityError，`typeof` 判断也会抛。`writeDemoSession` 无 try/catch → 登录提交静默无提示；`readDemoSession` 的 try 不覆盖 `typeof` 行 → AppLayout 门卫 effect 抛错。board 声称的修复 ③ 实际只处理了「localStorage 未定义」。演示日用 Playwright 干净 Chromium 不受影响，但声称与事实有差距。修法：两函数整体 try/catch，读失败/写失败都返回 null。
4. **`/api/cases` `take: 20` 无分页，铃铛统计直接消费**（已人工核实 route.ts:29 与 AppLayout.tsx:77）。活跃 Case 超 20 条时待处理/处理中/超7天计数静默偏低，/cases 也看不到第 21 条起。演示数据 6 条无影响。修法：铃铛改 count 聚合接口，列表加分页或放大上限。
5. **domain 包服务层是占位实现**（已人工核实 services.ts:7「伪代码」、`findDuplicateCandidates` 恒返回 `[]`）。生产 confirm/查重全在 `apps/web/src/lib`；README「项目结构」把「评分」归在 domain 名下与事实不符；误把 domain API 接进生产会得到恒空候选。修法：删死 API 或加显著 deprecation 注释，README 措辞对齐。
6. **`ConfirmIssueDecisionSchema` 是假承诺**（两路代理独立发现）。schemas.ts 注释称 REJECTED 必带 disposition 等，但字段 optional 无 superRefine，confirm 路由也不用它校验（实际约束在 confirm-intake-service 手搓；且 `edit.locationText` 无长度上限，契约写 200）。修法：schema 补 superRefine 并在路由入口 safeParse。
7. **展示材料同目录残留旧版含姓名讲稿**（已人工核实）。根目录 `02-初赛PPT逐页文案与讲稿.md` 含 4 处「乔瑞雪」「不理不器」，与现行材料同目录同名风格；preliminary README 只泛称「历史资料」。演示日打包易误取。修法：移历史子目录或文件头加显著废弃标注（对外材料归人拍板）。
8. **已闭合缺口仍被写成当前缺口**。TECH_SPEC:15,272（E2E 未隔离、Review 未展示 provider/model）、PRD:7、backlog B-7/8/9 均已落地但仍标待办。材料轮顺手勾销。
9. **walkthrough 断言恒真**（已人工核实 walkthrough-ui.mjs:178 `openRows >= 0`）。「24/24」中「状态筛选 OPEN 可用」一步永不可能失败；:124/:133 断言硬编码 true（仅靠异常兜底）。修法：改 `openRows > 0`。
10. **CI 的 E2E 跑 dev server 而非 build 产物**。server.mjs 起 `next dev`，前面 `pnpm -r build` 的产物未被 E2E 验证；README「全仓 build 与 Playwright E2E」并列易误读。修法：文档注明，或 CI 改 build+start。

### P3（打磨，酌情）

| # | 位置 | 问题 |
|---|---|---|
| 11 | packages/db/prisma/reset-demo.ts:23-32 | 声称清空所有业务数据，漏 CaseEmbedding、AiRun 两表（当前无写入路径，坑已埋） |
| 12 | packages/db/prisma/seed.ts | Intake 用 create 无幂等；ANALYZED/CONFIRMED 行缺配套分析/关联数据，仅 reset 路径安全 |
| 13 | duplicate-service.ts:98-107 | 地点分 0.5 边界非单调（同号 0.49→0.13 高于 0.50→0.10）；仅影响窄带排序，不自动合并红线未动 |
| 14 | confirm-intake-service.ts:74-80 | 幂等为事务内读后写（TOCTOU），SQLite 单写者下成立，迁 PostgreSQL 需改条件更新（与 analyze 路由 CAS 风格对齐） |
| 15 | duplicates/find route | `limit` 未夹取（负数时 `slice(0,-n)` 从尾部裁剪返回错误集合） |
| 16 | eval-provider.ts vs budget.ts | 声称支持 qwen/openai，传输层只放行 stepfun endpoint |
| 17 | openai-compatible.ts SYSTEM_PROMPT | 未声明 issues 最多 5 条，超限时烧光重试后 502 |
| 18 |杂项 | Toast id 用 Date.now 同毫秒碰撞且 type 未用（error 也绿✓）；ToastProvider/AIDraftCard/DuplicateCandidateCard 为死代码；settings 页 saveNotifyPrefs 在 setState updater 内副作用；dashboard 请求失败静默显示「—」；cases/new 错误展示优先裸错误码而非友好 message；eval 真实调用次数口径应为「两轮各 30 次」（文档多处写 30）；TASK.md 进度停在 09-05；README:114 旧括注「本次图片修复未重跑此包」；preliminary README 头部日期 08-31；`apps/web/playwright-report/`、`test-results/.last-run.json` 为生成产物却被跟踪；根 engines.node>=18 与 .nvmrc/CI 的 22 不一致 |

## 四、核对通过项（声明 vs 代码/实测）

- 三条产品底线未发现任何削弱实现：AI 产出仅草稿、确认/关联/状态全人工、文档数字与实测一致（213/99/32 实测吻合）。
- Confirm 原子事务三分支表覆盖完整、决策完整性/歧义/重复校验完备、字典外类别置空+审计留痕、AI 原值保留 IntakeIssue。
- 状态机 domain 单一来源、status 路由乐观锁 CAS 正确、非法迁移 422。
- analyze 超时接管（CAS 抢占+updatedAt 单调+失败恢复）无状态残留路径。
- 图片校验（1 张/JPG·PNG·WebP MIME+magic bytes/10MB/413）与文档一致；Mock 图片明确报错不冒充。
- Mock 降级双开关（DEMO_MODE ∧ AI_ALLOW_MOCK_FALLBACK）实现与文档一致；isVisionCapableModel 白名单正确。
- SYSTEM_PROMPT v2 注入防护规则 6/7 在代码中属实；body-timeout 覆盖正文读取、重试无副作用。
- E2E 隔离真实生效：3100 端口 + reuseExistingServer:false + 无条件覆盖 DATABASE_URL→e2e-demo.db + 强制 Mock（server.mjs 空值覆盖三把 key + 关降级开关）+ .next-e2e + reset.ts 双保险拒绝非 e2e 库；storageState gitignored。
- seed 与演示文档全对齐：6 Case / 8 Intake、CASE-018 IN_PROGRESS、CASE-011 硬负例、KPI 2/3/3、相似度 1.00 候选。
- CI 与 README 声称一致（typecheck→单测含 Eval→db:push→不变量→build→E2E，Node 22/pnpm 10，无密钥注入）；eval:quality 默认零外呼、--run 必须显式 --max-requests。
- 泄密扫描：全部材料电话 0 命中；07/08/DEMO_SCRIPT/narration/02-v2 无姓名（仅根 02 旧稿见 P2-7）；相对链接全量扫描 0 失效。

## 五、本报告完全没被此前会话记录过的新发现

（对照 state/board.md 各段）P1-2 的「非强制 Mock + 重置演示库」事实、P2-3 的 SecurityError 半修复、P2-4 的 take:20 统计失真、P2-5 的 domain 占位实现、P2-6 的契约假承诺、P3-13 的 0.5 边界非单调——均为本轮首次记录。E2E「口径差 1 条」board 凌晨 2 段已自承，本轮实测证实 32 为正确口径。

## 六、复测与元反思增补（2026-09-06 早，运行时验证）

### 6-1【P1 升级】不变量脚本的「基线恢复」是假消息，演示库会被真实污染（已实测证伪并修复）

- **实测**：复跑全量测试链后直接查 dev.db——14 cases / 33 intakes，KPI 实为 待处理 10 / 处理中 3 / 高优先级 9（应为 2/3/3）；残留夹具与 `test-confirm-invariants.mts` 用例一一对应（10000 字边界、「保留原文 电梯异常」、「manual 接管 / 旧请求成功」、「未分类事项」等）。
- **机制**：脚本 `finally` 只清理 `other-community` 跨组织数据（codex P2-4 的范围），它创建的 **demo-org 夹具全部留在 dev.db**，随后打印「🧹 Demo 基线恢复」——该消息失实。第二轮 reset 日志「cases: 删除 14 条 / intakes: 删除 33 条」证实污染自第一轮复跑（01:52）起一直存在。
- **影响**：跑过一次 `test:invariants` 之后直接演示，KPI 与事项列表全是测试夹具。CI 不受影响（临时环境）。
- **处置**：已 `db:reset` 恢复并逐项验证基线（6/8、2/3/3、CASE-018 IN_PROGRESS）。**修法建议**：脚本改用独立临时库（与 README 口径一致），或 finally 补全 demo-org 夹具清理；最低限度把消息改为「⚠ 演示库含测试夹具，演示前必须 db:reset」并写入 07 运行手册 T-60。
- **本报告勘误**：本报告此前依据脚本日志与代理代码审查推断「演示库已恢复基线」——未做运行时终态核验，判断错误，以本节实测为准。E2E 本身的隔离经本轮运行时复核**成立**（e2e-demo.db 独立文件、reset.ts 守卫拒绝非 E2E URL、server.mjs 强制覆盖 env）；污染源是不变量脚本，不是 E2E。

### 6-2 复测结果（第二轮全量，08:1x）

单测 213（25/33/74/81）、typecheck 0 错误、不变量 99/99、E2E 32 passed——与第一轮一致，两次独立复跑全绿。

### 6-3 本轮补查的上轮覆盖缺口（均 P3）

- `.env.example` 含 `NEXTAUTH_SECRET`/`NEXTAUTH_URL`/`STORAGE_*`——仓库无 next-auth 依赖、无存储层实现，属过时模板项。
- 仓库根 `next.config.js`（仅 reactStrictMode）为残留文件，真正配置在 apps/web；根目录还有历史 `.next/` 产物目录。
- `packages/ui` 为骨架包（自述未使用），但 package.json 声明了 radix/cva 等一批未用依赖。
- `scripts/check-tools.sh` 逻辑正确，无问题。

### 6-4 工作区归属待确认

出现两个非本会话创建的未跟踪文件：`docs/architecture/ARCHITECTURE_OVERVIEW.md`、`docs/architecture/架构通俗版.md`（生成于 2026-09-06 02:12）。内容与代码事实对齐（文档代理交叉核对口径一致），未引用本复审报告；判断为并行会话的「架构文档轮」产物，已原样保留、未提交，**归属请本人确认**。本会话新增/修改仅：本报告 + state/board.md 两条记录。

### 6-5 未复跑项（有意不做）

UI 全功能走查与真实模型冒烟（需消耗真实 StepFun 预算，授权归本人，属 07 手册 T-1 清单）；PPT/PDF/DOCX 二进制核对（归本人材料轮）。

