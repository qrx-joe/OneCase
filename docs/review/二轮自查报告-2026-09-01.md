# OneCase 二轮自查报告（阶段 1 + 审查整改后）

- **自查日期**: 2026-09-01
- **自查版本**: `61bb10a`（整改后）→ 自查中小修至本次提交
- **自查对象**: 阶段 1 全部提交（`647abbe`…`4070238`）与审查整改 4 项修复（`1e6aeee`/`c6b1d80`/`a84cd95`/`c10e466`）
- **自查方式**: 代码走读（重点：修复点的边界与层叠/键盘/状态机交互）、CSS 落点核对、定向回归

## 一、四项整改修复复核 —— 全部成立

| 修复 | 复核结论 |
|---|---|
| P1 首次查重失败不预填（`1e6aeee`） | 初始查询返回 `{ok, list, superseded}`；失败项写 `dupErrors` 并入 `skip`；被编辑顶替的结果同样不预填。刷新路径原有守卫未回退。E2E 覆盖"500 → 不预填 → 人工选择后可确认" |
| P2 搜索同步（`c6b1d80`） | `useEffect([searchParams])` 同步 q；E2E 的"第二次搜索列表断言"在无修复时必然失败，用例有效；后退恢复已覆盖 |
| P2 角标去重（`a84cd95`） | `total` 口径 = 命中任一条件的独立事项数；重叠场景单测明确（OPEN+停滞 → total 1）；aria-label 与显示同源 |
| P2 移动卡片标签（`c10e466`） | `td[data-label]::before` 规则确认位于 ≤768px 媒体块内（桌面不受影响）；E2E 以 `getComputedStyle(::before)` 断言标签内容 + 键盘 Enter 可达 |

层叠关系核对：processing 浮层 z-3000/2000 > notif-dropdown 200 > mobile-nav 150 > topbar 100，无遮挡倒置。

## 二、本轮新发现（小），已当场修复

1. **disposition E2E 的分析行查询未收敛**：`findFirstOrThrow({ where: { intakeId } })` 在分析重试产生多行时可能取到旧行——已加 `status: 'COMPLETED'` + `createdAt desc`。
2. **行卡片键盘只支持 Enter**：补 Space（两个列表页），符合原生控件习惯。
3. **首页移动端"···"操作列冗余**：整卡可点 + Enter 可达后该按钮无增量价值——移动端隐藏（`.cell-actions`），桌面保留。

## 三、记录进 backlog 的小项（不阻塞 Gate 1）

1. 首次查重失败后没有"重试候选"按钮，用户只能编辑草稿间接触发重查或直接人工选择（当前有失败提示 + 人工兜底，风险可接受）；
2. `AppLayout` 铃铛与页面各自请求 `/api/cases`，每次导航最多 2 次相同请求（MVP 可接受）；
3. "不建事项"选择面板无 `role="dialog"/radiogroup` 等 ARIA 语义。

## 四、自查后回归

- typecheck 通过；web 单测 33/33；
- 定向 E2E 6/6（移动黄金链路 / 业务出口 / 默认新建含失败分支 / 全局搜索）。

## 五、结论

四项整改修复实现与声明一致，未发现新的功能性缺陷；本轮小修 3 项 + backlog 3 项。外部报告第三节的试点阻断项（认证/对象级授权、编号并发、图片存储、真机与真人证据）维持阶段 3/4 排期。**代码侧已就绪，Gate 1（5 位社工脱敏可用性测试）待线下执行。**
